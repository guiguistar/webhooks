# Webhooks

Le test avec le content-type par défaut a fonctionné.

Le prochain push testera le content-type json/application.

Modification du webhook. Tests depuis PHP.

json_decode, 4 fois

Moodle should be reached for first time